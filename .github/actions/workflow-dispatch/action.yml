name: 'Workflow Dispatch'
description: 'Trigger another workflow'
author: 'Ephenodrom'
inputs:
  workflow:
    description: 'release'
    required: true
runs:
  using: 'composite'
  steps:
    - shell: bash
      run: echo "Triggering workflow ${{ inputs.workflow }}"
